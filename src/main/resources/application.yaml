file-upload:
  client-path: /file
  server-path: /Users/chanchhaya/Pictures/fileupload/
  base-uri: http://localhost:${server.port}${file-upload.client-path}

server:
  port: 9090
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/istad-oauth2
  jpa:
    hibernate:
      ddl-auto: update # create, drop, alter, truncate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:9432/db_ecommerce
    username: e1usr
    password: qwerqwer
  application:
    name: advanced-ecommerce-api-e1
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size:  20MB
keycloak:
  server-url: http://localhost:8080
  client-id: e1-spring-boot-client
  client-secret: XiKIj2DeHP62SnDl6MgKDefcGnZnrUjy
  realm: istad-oauth2

